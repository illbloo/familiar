{
  "name": "llm-familiar",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "argc build",
    "docker:start": "docker compose up -d",
    "mcp:start": "argc mcp start",
    "start": "bun run docker:start && bun run mcp:start",
    "check": "argc check",
    "client": "bun client/src/index.ts",
    "migrate-session": "bun scripts/session-migrator/index.ts",
    "notebook": "bun notebook/main.ts",
    "sampler": "bun sampler/src/cli.ts",
    "uithub": "bun tools/uithub/src/index.ts"
  },
  "workspaces": [
    "client",
    "memory",
    "mcp/bridge",
    "mcp/server",
    "midi",
    "notebook",
    "sampler",
    "scripts/session-migrator/*",
    "tools/uithub"
  ],
  "devDependencies": {
    "@types/bun": "^1.2.11"
  }
}
